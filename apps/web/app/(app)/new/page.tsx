import NovaConversa from "../../../components/NovaConversa";

/** Nova Conversa — type a name → provision a dev-first app (ADR 0038). */
export default function NewConversationPage() {
  return <NovaConversa />;
}
